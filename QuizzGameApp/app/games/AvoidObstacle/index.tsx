import { useGameSettings, SettingsDialog } from "./gameSettings";
import { useGame, Game } from "./game";
import config from "./config.json";

export { useGame, Game, useGameSettings, SettingsDialog, config };